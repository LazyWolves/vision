// This is the main package which contains the entry point to Vision
package main

// Import the vision API which serves requests
import (
	"vision/api"
)

// Main function which starts the API process
func main() {
	api.Api()
}
